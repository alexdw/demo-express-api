# ExpressJS, Mongoose and MoongoDB
### EOI DAW LEON  

Be sure that you have nodemon installed in global mode:

```sh
npm install nodemon -g
```

Install project's depencencies:

```sh
npm install express body-parser --save
```

And execute nodemon:

```sh
nodemon server.js
```